-- GoTeaLeaf Blackjack game using Sinatra server --
Version 1.0 - by Beengie

-- Currently using sinatra windows version --
1. Make sure the 'sinatra' gem is installed.
2. Make sure the 'sinatra-contrib'

